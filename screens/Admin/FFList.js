import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const FFList = () => {
    return (
        //use a similar code to the autofill screen
        <View>
            <Text></Text>
        </View>
    )
}

export default FFList

const styles = StyleSheet.create({})
